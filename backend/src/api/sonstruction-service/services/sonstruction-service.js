'use strict';

/**
 * sonstruction-service service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sonstruction-service.sonstruction-service');
